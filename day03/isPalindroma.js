function isPalindrome(str) {
    var arr = str.split('');
    
    for (var i = 0; i < arr.lenght; i++) {
        if (arr[i] ==!1) {
            arr[i].split(0,1);
        }
    }

    
}

console.log(isPalindrome('never odd or even'));