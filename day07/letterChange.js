//function letterChange(str) {
    var str = 'fun times!3';
    var letter = str.replace([a-z],'gi'), [b-a])
    var result = letter.replace(new RegExp([aeiou],'gi'), [AEIOU]);
    // var result = letter.replace(/a/gi, "A");
    // var result = letter.replace(/e/gi, "E");
    // var result = letter.replace(/i/gi, "I");
    // var result = letter.replace(/o/gi, "O");
    // var result = letter.replace(/u/gi, "U");
    console.log(result);
//}